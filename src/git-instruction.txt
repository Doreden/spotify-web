* push new changes to main: ~ git add .
                            ~ git commit -m "new commit message,"
                            ~ git push origin main 


* Get latest updates from main: ~ git pull

* Get latest updates from main to my branch:
                            ~ git branch
                            ~ git checkout your-branch
                            ~ git fetch origin main
                            ~ git merge origin/main

                        
